import React from 'react'

interface Props { }

function Hero(props: Props) {
    const { } = props

    return (
        <div className='bg-red-500 text-red-200'>
          Get 50% off when you shop
        </div>
    )
}

export default Hero
